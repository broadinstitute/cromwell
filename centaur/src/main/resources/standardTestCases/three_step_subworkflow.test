name: three_step__subwf_cwl
testFormat: workflowsuccess
workflowType: CWL
workflowTypeVersion: v1.0
backends: [Local]
tags: [localdockertest]

files {
  wdl: three_step/three_step_caller_wf.cwl,
  imports: [
    three_step/three_step.cwl
  ],
  // TODO CWL: Make CWLs work against the TES backend (this .options hard codes to Local)
  options: three_step/three_step.options
}

metadata {
  "submittedFiles.workflowType": CWL
  "submittedFiles.workflowTypeVersion": v1.0
# TODO CWL: Re-add this check when outputs are stable:
#  "outputs.count_output": "12"
}
