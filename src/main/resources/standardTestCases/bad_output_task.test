# Verifies that if we supply a non-existent filename to a File output variable that it fails
name: bad_output_task
testFormat: workflowfailure

files {
  wdl: bad_output_task/bad_output_task.wdl
}

metadata {
  workflowName: badExample
  status: Failed
}
