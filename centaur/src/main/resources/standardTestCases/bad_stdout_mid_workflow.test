# Verifies that if we use stdout in a workflow level, the workflow fails properly.
name: bad_stdout_mid_workflow
testFormat: workflowfailure

files {
  workflow: bad_stdouts/bad_stdout_mid_workflow.wdl
}

metadata {
  workflowName: bad_stdout
  status: Failed
}
