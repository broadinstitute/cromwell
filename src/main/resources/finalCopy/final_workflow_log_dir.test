# Tests that on a shared filesystem Cromwell is correctly localizing input files coming from the same as
# well as different directories. Tests both docker and non-docker

name: workflow_log_dir
testFormat: workflowsuccess
backends: [Local]
tags: [localdockertest]

files {
  wdl: final_workflow_log_dir/final_workflow_log_dir.wdl
  options: final_workflow_log_dir/final_workflow_log_dir.options
}

metadata {
  workflowName: wf
  status: Succeeded
}

