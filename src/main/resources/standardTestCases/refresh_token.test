name: refresh_token
testFormat: workflowsuccess
backends: [Jes-Refresh]

files {
  wdl: refresh_token/refresh_token.wdl
  inputs: refresh_token/refresh_token.inputs
  options: refresh_token/refresh_token.options
}

metadata {
  workflowName: hello
  status: Succeeded
  "calls.hello.hello.backend": "Jes-Refresh"
}
