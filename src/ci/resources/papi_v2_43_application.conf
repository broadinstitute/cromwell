include "papi_v2_application.conf"

database {
  profile = ${?CROMWELL_BUILD_CENTAUR_PRIOR_SLICK_PROFILE}
  db {
    driver = ${?CROMWELL_BUILD_CENTAUR_PRIOR_JDBC_DRIVER}
    url = ${?CROMWELL_BUILD_CENTAUR_PRIOR_JDBC_URL}
    user = ${?CROMWELL_BUILD_CENTAUR_PRIOR_JDBC_USERNAME}
    password = ${?CROMWELL_BUILD_CENTAUR_PRIOR_JDBC_PASSWORD}
  }
}
