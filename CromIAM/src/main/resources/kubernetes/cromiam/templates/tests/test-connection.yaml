apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cromiam.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "cromiam.name" . }}
    helm.sh/chart: {{ include "cromiam.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "cromiam.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
