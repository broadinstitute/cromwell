# Verifies that if we use stderr in a workflow level, the workflow fails properly.
name: bad_stderr_input
testFormat: workflowfailure

files {
    workflow: bad_stderrs/bad_stderr_input.wdl
}

metadata {
    workflowName: bad_stderr
    status: Failed
}
